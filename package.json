{
	"name": "puppeteer-bench",
	"version": "1.0.0",
	"description": "Benchmarking Front End Interactions",
	"main": "index.js",
	"scripts": {
		"full-test": "sh build.sh",
		"bench": "npm run tsc && node index.js",
		"tsc": "tsc index.ts",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Torre Taylor",
	"license": "ISC",
	"dependencies": {
		"@types/puppeteer": "^3.0.1",
		"puppeteer": "^5.2.1"
	},
	"devDependencies": {
		"@tsconfig/node12": "^1.0.7"
	}
}
